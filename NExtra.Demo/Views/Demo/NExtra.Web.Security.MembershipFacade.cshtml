@model DemoModel

@Model.GetTypeSummary(typeof(MembershipFacade))

<p>@Html.GlobalResource(Resources.Language.CodeSample):</p>

<div class="container code">
  <pre>
var facade = new MembershipFacade();

var minLength = facade.MinPasswordLength;

var result  = facade.ChangePassword("daniel", "oldPwd", "newPwd");
var status  = facade.CreateUser("daniel", "password", "daniel@saidi.se");
var users   = facade.FindUsersByName("daniel");
var users   = facade.FindUsersByEmail("daniel.saidi@gmail.com");
var current = facade.GetUser();
var user    = facade.GetUser("daniel");
var newPwd  = facade.ResetPassword("daniel");
var valid   = facade.Validate("daniel", "pwd");

facade.DeleteUser("daniel");
facade.SetIsApproved("daniel", false);</pre>
</div>